<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vixeloe Adverts</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #f06, #004d7a);
            margin: 0;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        a:first-child {
            margin-bottom: 30px;
            margin-right: auto;
        }

        a img{
            height: 30px;
            width:  auto
        }

        .container {
            background: #fff;
            padding: 60px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            border-radius: 10px; 
            max-width: 800px;
            width: 100%;
            animation: fadeIn 0.5s ease-in-out;
        }

        h1 {
            margin-bottom: 40px;
            font-weight: 700;
            color: #333;
            font-family: 'Open Sans', sans-serif;
            font-size: 20px;
            font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 5px;
            font-weight: 600;
            color: #333333d6;
        }

        select{
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }
        
        select option{
            font-size: 14px;
        }

        input, textarea, select {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        input:focus, textarea:focus, select:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        button {
            padding: 15px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s;
            font-weight: 700;
        }

        button:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        .success-message, .error-message {
            display: none;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .success-message {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .error-message {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

    
        .btm{
            margin-block: 40px;
            background-color: #fff;
            width: 100%;
            max-width: 800px;
            padding: 20px 60px;
            border-radius: 5px;
            font-size: 14px;
        }

        @media (max-width: 1000px){
            .btm{
                width: 80%;
            }
        }

        .btm a{
            color: unset;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .btm a:hover{
            color: orange;
            text-decoration: underline;
        }

        #msg-cut{
            background-color: orange;
            color: #fff;
            padding: 10px;
            border-radius: 60px;
            position: fixed;
            right: 20px;
            bottom: 20vh;
            box-shadow: 1px 1px 5px #000;
            animation: bounce 1s 3s forwards 10;
            animation: name duration timing-function delay iteration-count direction fill-mode;
            animation-fill-mode: both;
            animation-play-state: running;
        }

        @keyframes bounce {
            20%{
                transform: rotate(10deg);
            }
            40%{
                transform: rotate(-10deg);
            }
            60%{
                transform: rotate(10deg);
            }
            80%{
                transform: rotate(0px);
            }
            100%{
                transform: rotate(0px);
            }
            
        }

        .head{
            width: 100%;
            display: flex;
            padding-top: 20px;
        }

        .hmxX{
            background-color: #bcbddd65;
            height: fit-content;
            padding: 5px;
            border-radius: 50%;
            transition: all 0.3s ease;
        }

        .hmxX:hover{
            transform: scale(1.1);
            box-shadow: 1px 1px 3px #000;
            background-color: lightblue;
        }
    </style>
</head>
<body>
    <div class="head">
        <a href="/index">
            <img src="/img/vixeloe-logo2b-removebg-preview.png" alt="">
        </a>
        <div class="hmxX">
            <a href="/index">
                <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#fff"><path d="M240-200h120v-240h240v240h120v-360L480-740 240-560v360Zm-80 80v-480l320-240 320 240v480H520v-240h-80v240H160Zm320-350Z"/></svg>
            </a>
        </div>
    </div>

<div class="container">
    <h1>ADVERTISE WITH US</h1>
   
    <form id="adForm">
        <label for="company">Company Name:</label>
        <input type="text" id="company" name="company" required>

        <label for="email">Company Email Address:</label>
        <input type="email" id="email" name="email" required>

        <label for="name">Delegate's Full Name:</label>
        <input type="text" id="name" name="name" required>

        <label for="phone">Phone Number:</label>
        <input type="tel" id="phone" name="phone" required>


        <label for="budget">Budget:</label>
        <select id="budget" name="budget" required>
            <option value="" disabled selected>Select your budget</option>
            <option value="1000">$1,000 - $5,000</option>
            <option value="5000">$5,000 - $10,000</option>
            <option value="10000">$10,000 - $50,000</option>
            <option value="50000">Over $50,000</option>
        </select>

        <label for="message">Message:</label>
        <textarea id="message" name="message" rows="5" required></textarea>

        <div class="success-message" id="success-message">Your message has been sent successfully!</div>
        <div class="error-message" id="error-message">There was an error sending your message. Please try again.</div>

        <button type="submit">Send Message</button>
    </form>
</div>


<div class="btm">
    <div>
        Need help on advertising?
        Write to <a href="">vixeloeadverts@gmail.com</a>.
    </div>
    <br>
    <a href="">Complaints and suggestions</a>.
</div>

<div id="msg-cut">
    <i class="fa-solid fa-messsage"></i>
    <span>Message Us</span>
</div>

<script>
    document.getElementById('adForm').addEventListener('submit', function(event) {
        event.preventDefault();

        // Hide previous messages
        document.getElementById('success-message').style.display = 'none';
        document.getElementById('error-message').style.display = 'none';

        // Example form validation and submission
        const formData = new FormData(this);
        fetch('your-backend-url-here', {
            method: 'POST',
            body: formData
        })
        .then(response => {
            if (response.ok) {
                document.getElementById('success-message').style.display = 'block';
                this.reset();
            } else {
                document.getElementById('error-message').style.display = 'block';
            }
        })
        .catch(error => {
            document.getElementById('error-message').style.display = 'block';
        });
    });
</script>

</body>
</html>
